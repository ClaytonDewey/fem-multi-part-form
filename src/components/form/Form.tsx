import { FormButtons, Personal } from '.';

const Form = () => {
  return (
    <section className='form'>
      <form>
        <Personal />
        <FormButtons />
      </form>
    </section>
  );
};
export default Form;
